<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naskah.demo.mapper.NoteMapper">

    <select id="findByUserBookAndPage" resultType="com.naskah.demo.model.entity.Note">
        SELECT
        id, user_id, book_id, page, position, title,
        content, color, is_private, created_at, updated_at
        FROM notes
        WHERE user_id = #{userId}
        AND book_id = #{bookId}
        <if test="page != null">
            AND page = #{page}
        </if>
        ORDER BY created_at DESC
    </select>
</mapper>