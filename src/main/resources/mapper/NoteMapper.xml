<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.masasilam.app.mapper.book.NoteMapper">
    <insert id="insertNote" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notes (user_id, book_id, chapter_number, chapter_title, chapter_slug, start_position, end_position,
        content, selected_text, created_at, updated_at)
        VALUES (#{userId}, #{bookId}, #{chapterNumber}, #{chapterTitle}, #{chapterSlug}, #{startPosition},
        #{endPosition}, #{content}, #{selectedText}, #{createdAt}, #{updatedAt})
    </insert>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notes (user_id, book_id, chapter_number, note_text, note_type, is_draft, created_at)
        VALUES (#{userId}, #{bookId}, #{chapterNumber}, #{noteText}, #{noteType}, #{isDraft}, NOW())
    </insert>

    <update id="update">
        UPDATE notes
        SET note_text = #{noteText},
        note_type = #{noteType},
        is_draft = #{isDraft},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM notes
        WHERE id = #{id}
    </delete>

    <delete id="deleteNote">
        DELETE FROM notes
        WHERE id = #{id}
    </delete>

    <select id="findNoteById" resultType="com.masasilam.app.model.entity.Note">
        SELECT *
        FROM notes
        WHERE id = #{id}
    </select>

    <select id="findById" resultType="com.masasilam.app.model.entity.Note">
        SELECT *
        FROM notes
        WHERE id = #{id}
    </select>

    <select id="findByUserBookAndChapter" resultType="com.masasilam.app.model.entity.Note">
        SELECT *
        FROM notes
        WHERE user_id = #{userId}
        AND book_id = #{bookId}
        AND chapter_number = #{chapterNumber}
        ORDER BY created_at DESC
    </select>

    <select id="findByUser" resultType="com.masasilam.app.model.entity.Note">
        SELECT *
        FROM notes
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="findByUserAndBook" resultType="com.masasilam.app.model.entity.Note">
        SELECT *
        FROM notes
        WHERE user_id = #{userId}
        AND book_id = #{bookId}
        ORDER BY chapter_number, created_at DESC
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*)
        FROM notes
        WHERE user_id = #{userId}
    </select>
</mapper>