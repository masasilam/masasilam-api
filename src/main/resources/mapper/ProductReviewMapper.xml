<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.masasilam.app.mapper.kios.ProductReviewMapper">
    <insert id="insertReview" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_reviews (user_id, product_id, rating, comment, created_at, updated_at)
        VALUES (#{userId}, #{productId}, #{rating}, #{comment}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByProductId" resultType="com.masasilam.app.model.entity.ProductReview">
        SELECT *
        FROM product_reviews
        WHERE product_id = #{productId}
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findByUserAndProduct" resultType="com.masasilam.app.model.entity.ProductReview">
        SELECT *
        FROM product_reviews
        WHERE user_id = #{userId}
        AND product_id = #{productId}
    </select>
</mapper>