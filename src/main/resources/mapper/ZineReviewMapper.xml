<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.masasilam.app.mapper.zine.ZineReviewMapper">
    <resultMap id="ZineReviewResultMap" type="com.masasilam.app.model.entity.ZineReview">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="zineId" column="zine_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="helpfulCount" column="helpful_count"/>
        <result property="notHelpfulCount" column="not_helpful_count"/>
        <result property="replyCount" column="reply_count"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO zine_reviews (
        user_id, zine_id, title, content,
        helpful_count, not_helpful_count, reply_count,
        is_deleted, created_at, updated_at
        ) VALUES (
        #{userId}, #{zineId}, #{title}, #{content},
        #{helpfulCount}, #{notHelpfulCount}, #{replyCount},
        false, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update">
        UPDATE zine_reviews
        SET title = #{title},
        content = #{content},
        updated_at = #{updatedAt}
        WHERE id = #{id}
        AND is_deleted = false
    </update>

    <update id="softDelete">
        UPDATE zine_reviews
        SET is_deleted = true,
        deleted_at = NOW()
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="ZineReviewResultMap">
        SELECT * FROM zine_reviews
        WHERE id = #{id}
        AND is_deleted = false
    </select>

    <select id="findByUserAndZine" resultMap="ZineReviewResultMap">
        SELECT * FROM zine_reviews
        WHERE user_id = #{userId}
        AND zine_id = #{zineId}
        AND is_deleted = false
    </select>

    <select id="findByZineWithPagination" resultMap="ZineReviewResultMap">
        SELECT * FROM zine_reviews
        WHERE zine_id = #{zineId}
        AND is_deleted = false
        <choose>
            <when test="sortBy == 'helpful'">
                ORDER BY helpful_count DESC, created_at DESC
            </when>
            <when test="sortBy == 'oldest'">
                ORDER BY created_at ASC
            </when>
            <otherwise>
                ORDER BY created_at DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByZine" resultType="int">
        SELECT COUNT(*) FROM zine_reviews
        WHERE zine_id = #{zineId}
        AND is_deleted = false
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*) FROM zine_reviews
        WHERE user_id = #{userId}
        AND is_deleted = false
    </select>

    <select id="findByUser" resultMap="ZineReviewResultMap">
        SELECT * FROM zine_reviews
        WHERE user_id = #{userId}
        AND is_deleted = false
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>