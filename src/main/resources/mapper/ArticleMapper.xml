<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.masasilam.app.mapper.newspaper.ArticleRatingMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article_ratings (user_id, article_id, rating, created_at, updated_at)
        VALUES (#{userId}, #{articleId}, #{rating}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE article_ratings
        SET rating = #{rating},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM article_ratings
        WHERE id = #{id}
    </delete>

    <select id="findByUserAndArticle" resultType="com.masasilam.app.model.entity.newspaper.ArticleRating">
        SELECT *
        FROM article_ratings
        WHERE user_id = #{userId}
        AND article_id = #{articleId}
    </select>

    <select id="getArticleRatingStats" resultType="com.masasilam.app.model.dto.newspaper.ArticleRatingStatsResponse">
        SELECT article_id AS articleId,
        AVG(rating) AS averageRating,
        COUNT(*) AS totalRatings,
        SUM(CASE WHEN rating = 5 THEN 1 ELSE 0 END) AS fiveStarCount,
        SUM(CASE WHEN rating = 4 THEN 1 ELSE 0 END) AS fourStarCount,
        SUM(CASE WHEN rating = 3 THEN 1 ELSE 0 END) AS threeStarCount,
        SUM(CASE WHEN rating = 2 THEN 1 ELSE 0 END) AS twoStarCount,
        SUM(CASE WHEN rating = 1 THEN 1 ELSE 0 END) AS oneStarCount
        FROM article_ratings
        WHERE article_id = #{articleId}
        GROUP BY article_id
    </select>
</mapper>