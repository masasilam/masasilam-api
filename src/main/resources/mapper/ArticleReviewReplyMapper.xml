<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.masasilam.app.mapper.newspaper.ArticleReviewReplyMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article_review_replies (user_id, review_id, parent_reply_id, content, created_at, updated_at)
        VALUES (#{userId}, #{reviewId}, #{parentReplyId}, #{content}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE article_review_replies
        SET content = #{content},
        updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="softDelete">
        UPDATE article_review_replies
        SET is_deleted = TRUE
        WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.masasilam.app.model.entity.newspaper.ArticleReviewReply">
        SELECT *
        FROM article_review_replies
        WHERE id = #{id}
        AND is_deleted = FALSE
    </select>

    <select id="findByReviewId" resultType="com.masasilam.app.model.entity.newspaper.ArticleReviewReply">
        SELECT *
        FROM article_review_replies
        WHERE review_id = #{reviewId}
        AND is_deleted = FALSE
        ORDER BY created_at ASC
    </select>
</mapper>